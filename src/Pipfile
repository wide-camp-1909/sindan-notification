[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
requests = "*"
pyyaml = "*"

[requires]
python_version = "3.7"

[scripts]
notifier = "python notifier.py"
proxy = "python proxy.py"
